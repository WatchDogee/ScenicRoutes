{
  "version": 1,
  "type": "docker-compose",
  "services": {
    "app": {
      "port": 3000,
      "environment": [
        "APP_ENV=production",
        "APP_DEBUG=true",
        "APP_URL=${APP_URL}",
        "DB_CONNECTION=mysql",
        "DB_HOST=${DB_HOST}",
        "DB_PORT=${DB_PORT}",
        "DB_DATABASE=${DB_DATABASE}",
        "DB_USERNAME=${DB_USERNAME}",
        "DB_PASSWORD=${DB_PASSWORD}"
      ],
      "volumes": [
        {
          "source": ".",
          "target": "/app"
        },
        {
          "source": "app_storage",
          "target": "/app/storage"
        },
        {
          "source": "app_bootstrap_cache",
          "target": "/app/bootstrap/cache"
        }
      ],
      "healthcheck": {
        "test": ["CMD-SHELL", "curl -f http://localhost/health || exit 0"],
        "interval": "10s",
        "timeout": "5s",
        "retries": 3,
        "start_period": "30s"
      }
    }
  },
  "volumes": ["app_storage", "app_bootstrap_cache"],
  "networks": ["app_network"],
  "deploy": {
    "command": [
      "ls -la /app",
      "mkdir -p /app/storage/logs",
      "mkdir -p /app/storage/framework/sessions",
      "mkdir -p /app/storage/framework/views",
      "mkdir -p /app/storage/framework/cache",
      "chmod -R 777 /app/storage",
      "chmod -R 777 /app/bootstrap/cache",
      "touch /app/storage/logs/laravel.log",
      "chmod 666 /app/storage/logs/laravel.log",
      "cp /app/.env.coolify /app/.env",
      "cd /app && php /app/artisan key:generate --force",
      "cd /app && php /app/artisan config:clear",
      "cd /app && php /app/artisan cache:clear",
      "cd /app && php /app/artisan view:clear",
      "cd /app && php /app/artisan route:clear",
      "cd /app && php /app/artisan storage:link",
      "cd /app && php /app/artisan migrate --force",
      "cd /app && npm install",
      "cd /app && npm run build",
      "chown -R www-data:www-data /app/storage /app/bootstrap/cache"
    ]
  }
}


